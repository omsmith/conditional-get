{
  "name": "koa-conditional-get",
  "description": "Conditional GET support for koa",
  "repository": "koajs/conditional-get",
  "version": "1.0.3",
  "keywords": [
    "koa",
    "middleware",
    "cache",
    "caching",
    "etag"
  ],
  "files": [
    "index.js"
  ],
  "devDependencies": {
    "istanbul-harmony": "0",
    "koa": "^2.0.0-alpha.3",
    "koa-etag": "git+ssh://git@github.com/omsmith/etag.git#koa2",
    "mocha": "1",
    "should": "3",
    "supertest": "0"
  },
  "license": "MIT",
  "dependencies": {},
  "scripts": {
    "test": "NODE_ENV=test mocha --require should --reporter spec",
    "test-cov": "NODE_ENV=test node ./node_modules/.bin/istanbul cover ./node_modules/.bin/_mocha -- --require should",
    "test-travis": "NODE_ENV=test node ./node_modules/.bin/istanbul cover ./node_modules/.bin/_mocha --report lcovonly -- --require should"
  }
}
